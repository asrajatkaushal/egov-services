CREATE TABLE eg_calendarYear (
	id BIGINT NOT NULL,
	name INTEGER NOT NULL,
	startDate DATE NOT NULL,
	endDate DATE NOT NULL,
	active BOOLEAN NOT NULL,
	tenantId CHARACTER VARYING(250) NOT NULL,

	CONSTRAINT pk_eg_calendarYear PRIMARY KEY (id),
	CONSTRAINT uk_eg_calendarYear_name UNIQUE KEY (name)
);

CREATE SEQUENCE seq_eg_calendarYear INCREMENT 1 MINVALUE 1 MAXVALUE 9223372036854775807 START 1 CACHE 1;